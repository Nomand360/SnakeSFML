#ifndef GAME_H
#define GAME_H
#include "window.h"
#include "snake.h"
#include "apple.h"

class Game
{
public:
    Game();
    Game(const Game &gm) = delete;
    Game &operator=(const Game &gm) = delete;
    ~Game();
private:
    Window window;
    Snake snake;
};

#endif // GAME_H
